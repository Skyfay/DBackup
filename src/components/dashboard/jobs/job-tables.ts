/**
 * The name under which each user's column layout of the job table is saved, and the page id of
 * their choice of table or cards. A plain module, so the server page can load both.
 */
export const JOBS_TABLE_ID = "jobs";
export const JOBS_PAGE_ID = "jobs";
