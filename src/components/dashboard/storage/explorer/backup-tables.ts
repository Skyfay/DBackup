/**
 * The name under which each user's column layout of the list of backups is saved, and the page ids
 * of their choice of table or timeline in both tabs. A plain module, so the server page can load them.
 */
export const BACKUPS_TABLE_ID = "storage.backups";
export const BACKUPS_PAGE_ID = "storage.backups";
export const DESTINATIONS_PAGE_ID = "storage.destinations";
