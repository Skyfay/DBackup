/**
 * The name under which each user's column layout of the list of backups is saved, and the page id
 * of their choice of table or cards. A plain module, so the server page can load both.
 */
export const BACKUPS_TABLE_ID = "storage.backups";
export const BACKUPS_PAGE_ID = "storage.backups";
